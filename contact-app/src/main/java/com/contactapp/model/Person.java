package com.contactapp.model;

import java.time.LocalDate;

/**
 * Model class representing a person in the contact app.
 * Maps directly to the `person` table in the SQLite database.
 *
 * TASK 1 — Fill in this class:
 *   - Add all fields matching the DB schema
 *   - Add a full constructor, a no-arg constructor
 *   - Add getters and setters for every field
 *   - Optionally override toString() for debugging
 */
public class Person {

    // TODO: Add fields
    // idperson (int), lastname (String), firstname (String), nickname (String),
    // phone_number (String), address (String), email_address (String), birth_date (LocalDate)

    // TODO: Add constructors

    // TODO: Add getters and setters

}
